services:
  - type: web
    name: fitness-backend
    env: node
    buildCommand: npm install --no-audit --no-fund && npx prisma migrate deploy
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production